This demonstrates two confusing problems. 

 #.  Why do map reduce jobs run in the SYS pool instead of the PUBLIC pool?
 #.  Any map reduce job that writes or reads from igfs will cause a deadlock .
 

